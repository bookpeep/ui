import { Components } from "@mui/material";

const MuiSelect: Components["MuiSelect"] = {
  styleOverrides: {
    select: {
      padding: "14px 12px",
      minWidth: "100px",
    },
  },

  defaultProps: {},

  variants: [
    {
      props: {},
      style: {},
    },
  ],
};

export default MuiSelect;
